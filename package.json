{
  "name": "simed-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "clean:ports": "npx kill-port 3000 5173",
    "dev": "npm run clean:ports && concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run clean:ports && npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}